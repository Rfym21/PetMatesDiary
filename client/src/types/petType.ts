export type PetType = {
  images: string[] | null;           // 图片
  species: string | null;            // 物种
  coat: string | null;               // 毛发长短
  fur_type: string | null;           // 毛发类型(蓬松;卷毛;顺滑)
  fur_color: string | null;          // 毛发颜色
  temperament: string | null;        // 目测性格
  size: string | null;               // 体型
  limbs: string | null;              // 四肢长短
  ears: string | null;               // 耳部现状
  tail: string | null                // 尾部
}
